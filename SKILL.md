---
name: gzh-minimal-layout
description: 分析 Markdown 或纯文本公众号文章的结构、语义与阅读节奏，选择已注册主题，并生成可直接粘贴到微信公众号编辑器的内联 HTML。用户提出公众号排版、微信文章排版、文章主题预览、文章结构拆分合并或 375px 微信排版时使用。
---

# 公众号智能排版

你是排版 Agent：阅读文章、理解作者已经写下的结构与标记、选择主题，并把它们组织成一篇可读的公众号文章。本 Skill 的主题包和渲染内核负责把这个判断稳定地落实为微信兼容 HTML；它们是内部执行能力，不是用户需要学习的一组 CLI 命令。

## 先守住这些边界

- 原始文章只读：不得改写、补写、删减、重复或调换正文。可调整的只有相邻内容的分块边界与块间空白。
- 主题可渲染一层**派生版式外壳**，但它不属于正文：引言只可摘取首个 lead 的首句，目录只可读取已识别标题；章节英文标签、`END`、作者占位与 CTA 必须来自主题的 `composition.chrome`。标题中的原始序号是结构标记：当主题已经提供章节标记/英文标签时，标题文本槽位必须隐藏它，避免重复；标题文字本身不得改写。其余外壳绝不进入或替代任何原文块，完整性校验仍只对原文块负责。
- 作者已有的 `**加粗**`、`*强调*`、`==高亮==`、`<u>下划线</u>` / `++下划线++`、`~~删除线~~`、行内代码必须保留其语义；由主题的 `inlineMarks` 统一给样式。正文可在 `composition.inlineMarkBudget` 内补充少量、可解释的语义标记：概念词用主题色加粗，关键数据/时长用浅底高亮；绝不改写或替换原文。
- 先做全文判断，再排块。普通标题、正文、列表、引用、代码、表格和结尾由主题配方的默认映射处理；不要逐段随机挑卡片或组件。
- 阅读手势只控制间距。`pause`、`pivot`、`release` 不能让普通正文批量出现竖线、底色或卡片。
- 375px 输出的外层左右内容轨固定为 20px。普通正文不得额外整体缩进。
- 不上传、不发布、不创建草稿；除非用户明确要求，且当前环境另有相应能力。

## 面向文章的工作流

### 1. 得到一份可审阅源文

Markdown 可直接使用。纯文本、DOCX、HTML 或网页富文本先转成独立的 Markdown 草稿；完整输入边界见 [输入工作流](references/input-workflow.md)。PDF 必须由具备页面读取能力的宿主先提取，不能用二进制猜测文本顺序。

归一化后的草稿是新的候选源文：通读它，确认标题、图片、表格、段落顺序和转换 warning。把中间产物放在临时目录，不写回用户的原文件。

### 2. 读全文，再选文章的阅读语言

先完整阅读源文，然后读取 [主题索引](references/theme-index.md) 和候选主题的 `references/themes/<theme-id>.md`。用户指定主题时直接采用；未指定时根据全文的文章类型、语气、结构和阅读速度选择一个主题，并简短说明取舍。

主题说明不是灵感板：其中的 `composition`、recipe 和映射规定了这篇文章的阅读语言。不要混用主题，也不要只因某一段好看而换主题。

### 3. 用语义而非装饰组织排版

按原文顺序识别文章标题、章节标题、连续正文、列表、引用、代码、图片、表格、分隔线、收束和 CTA；只可合并相邻、语义连续的内容，不能跨章节。纯文本的单次换行只是候选阅读节拍：同一主语、同一论证方向的 2–3 句应合并为普通正文组；转折句、连续反问、因果链应独立成组；“第一/第二/第三……”这类行动小主题投射为 H3，而不是普通段落或列表。只有作者真实写出项目符号、`1.` 或 `一、` 等列表标记时才能渲染成列表，不能因为短句连续就造 list。

重点块只留给真正的判断、转折或结论，通常 0–3 处，并服从主题 recipe 的上限。作者已有的行内标记照原样渲染；对完整、自洽的核心判断，可映射到主题 `focus` 组件。行内自动标记独立受主题预算约束：连续正文组最多 4 个、最多两种样式、浅底高亮最多 1 个；概念短语严格选择 3–15 字，优先完整动作、核心判断、关键数据和专有名词，没有明确重点则不标。

### 4. 在内部执行确定性渲染

将上面的排版判断转换为内部渲染计划，交给主题映射和渲染器执行；Agent 不手写 HTML/CSS。内部计划会绑定源文 hash，保留每个语义块和 mark，并由内核选择普通组件。技术维护者需要命令、决策合约或故障排查时，才读取 [内部渲染与验证契约](references/agent-workflow.md)。

每次输出都必须通过：源文完整性、渲染内容完整性和微信 HTML 兼容性检查。渲染结果里的 `derivedChrome` 要明确显示这层外壳实际启用了哪些项；出现问题时，优先修正分块、语义角色、主题选择或极少数重点判断；只有重复出现同一主题缺陷时才改主题资产。

### 5. 预览并交付

检查整篇 375px 预览，不只看首屏：

- 普通正文是否保持连续阅读，左右是否仍在 20px 内容轨；
- 标题、章节、列表、引用、结尾是否形成清晰层级；
- 列表、引用署名、图片、表格和作者标记是否完整保留；
- 是否出现大量孤立单句、错误跨章合并，或相邻/过多的强组件；
- 长句、英文、数字、代码和标点是否溢出。

交付 `<slug>.wechat.html` 和带“一键复制到公众号”按钮的 `<slug>.preview.html`。说明所选主题、文章级排版判断及两项完整性校验结果；内部 JSON 只在用户要求审计或排错时提供。预览按钮和脚本不得被复制进公众号。

## 主题资产与维护

主题包是唯一设计源：`.themes/<theme-id>/theme.json` 定义设计变量、`inlineMarks`、`composition.inlineMarkBudget`、composition recipe、语义映射与 `composition.chrome` 派生外壳，`components/*/{component.json,template.html}` 定义可执行组件。`references/themes/<theme-id>.md` 由这些真实资产生成，只读、不作为第二套源码维护。

改主题时，运行：

```bash
npm run lint:themes
npm run docs:themes
npm run build
npm test
```

`lint:themes` 会验证主题包、recipe/映射引用和微信兼容边界，并拒绝正文中的绝对定位、浮动、网格和预格式化空白。完整主题资产闭环见 [主题工作流](references/theme-workflow.md)；新增主题见 [自定义主题工作流](references/custom-theme-workflow.md)。
